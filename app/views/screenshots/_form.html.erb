<%= form_with(model: screenshot) do |form| %>
  <% if screenshot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(screenshot.errors.count, "error") %> prohibited this screenshot from being saved:</h2>

      <ul>
        <% screenshot.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="field">
    <%= form.label :link_id %>
    <%= form.text_field :link_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
